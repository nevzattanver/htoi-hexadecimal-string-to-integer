# htoi-hexadecimal-string-to-integer
htoi function that converts the hexadecimal string to its integer value.

- Works with 0x... or 0X... input format.
- Function args : char* str
- Return type : int
